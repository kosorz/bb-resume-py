import styled from "styled-components";

const SubPickHeadline = styled.h4`
  margin: 0;
  font-weight: lighter;
  padding-bottom: ${({ theme }) => theme.spaceSmall / 4 + "px"};
  font-size: 16px;
`;

export default SubPickHeadline;
